services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - chat-api
    restart: unless-stopped

  chat-api:
    build:
      context: ./api
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: unless-stopped
